document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault(); // Isso impede que o HTML envie o formulário do jeito padrão
    
    // Seu código de salvamento aqui...
});
